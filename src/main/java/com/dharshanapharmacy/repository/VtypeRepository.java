package com.dharshanapharmacy.repository;

import com.dharshanapharmacy.model.Vtype;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VtypeRepository extends JpaRepository<Vtype,Integer> {
}
